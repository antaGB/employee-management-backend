# Express TypeScript API

A robust, type-safe REST API boilerplate built with **Node.js**, **Express**, and **TypeScript**. This project features JWT-based authentication, role-based authorization, and clean architectural patterns.

## 🚀 Features

- **TypeScript**: Static typing for better developer experience and fewer bugs.
- **Authentication**: Secure login and registration using `bcryptjs` and `jsonwebtoken`.
- **Authorization**: Middleware for Role-Based Access Control (RBAC).
- **Security**: CORS configuration and environment variable protection.
- **Validation**: Centralized type definitions for Express Requests.

---

## 🛠️ Tech Stack

* **Runtime**: Node.js
* **Framework**: Express.js
* **Language**: TypeScript
* **Auth**: JWT (JSON Web Tokens)
* **Tooling**: Nodemon, ts-node, Dotenv

---

## 📋 Prerequisites

- Node.js (v16 or higher)
- npm or yarn

---